# ===================================
# Research Paper Analysis & Critique
# Environment Configuration Template
# ===================================

# Flask Configuration
SECRET_KEY=your-super-secret-key-here-change-in-production
JWT_SECRET_KEY=your-jwt-secret-key-here-change-in-production
FLASK_ENV=development
FLASK_DEBUG=true

# Database Configuration
# For local development (SQLite)
SQLALCHEMY_DATABASE_URI=sqlite:///instance/app.db
# For production (PostgreSQL)
# SQLALCHEMY_DATABASE_URI=postgresql://username:password@localhost/dbname

# File Upload Settings
UPLOAD_DIR=uploads
REPORT_DIR=reports
CORPUS_DIR=corpus
MAX_UPLOAD_MB=25
ALLOWED_EXT=.pdf

# AI & API Configuration
# ----------------------

# HuggingFace Model Settings
USE_HF_SUMMARIZER=true
HF_MODEL_NAME=facebook/bart-large-cnn
HF_CACHE_DIR=./models_cache

# Google Fact Check Tools API
# Get your free API key from: https://console.cloud.google.com/
GOOGLE_FACT_CHECK_API_KEY=your-google-fact-check-api-key-here
FACTCHECK_USE=api_key
FACTCHECK_TIMEOUT=8.0
FACTCHECK_MAX_RETRIES=3
FACTCHECK_DELAY=0.5

# Optional: Google Service Account (alternative to API key)
# GOOGLE_FACTCHECK_SERVICE_ACCOUNT_FILE=path/to/service-account.json
# FACTCHECK_USE=service_account

# CrossRef API (Free - No Key Required)
# CrossRef is used for citation validation and requires no API key
CROSSREF_API_URL=https://api.crossref.org/works

# Semantic Scholar API (Optional)
SEMANTIC_SCHOLAR_BASE=https://api.semanticscholar.org/graph/v1/paper/search
SEMANTIC_SCHOLAR_FIELDS=title,authors,year,venue
SEMANTIC_SCHOLAR_KEY=your-semantic-scholar-key-here-optional

# Security & CORS Settings
# ------------------------
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
ALLOW_GUEST_UPLOADS=false

# JWT Token Expiration (in seconds)
JWT_ACCESS_TOKEN_EXPIRES=3600
JWT_REFRESH_TOKEN_EXPIRES=2592000

# Production Settings
# -------------------
# Uncomment and configure for production deployment

# SSL/HTTPS
# FORCE_HTTPS=true
# SESSION_COOKIE_SECURE=true
# SESSION_COOKIE_HTTPONLY=true

# Rate Limiting
# RATELIMIT_STORAGE_URL=redis://localhost:6379

# Email Configuration (for password reset, notifications)
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=true
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password

# Monitoring & Analytics
# SENTRY_DSN=your-sentry-dsn-for-error-tracking

# ===================================
# Setup Instructions:
# ===================================
# 1. Copy this file to '.env' in your project root
# 2. Replace all placeholder values with your actual configuration
# 3. For Google Fact Check API:
#    - Go to https://console.cloud.google.com/
#    - Enable the "Fact Check Tools API"
#    - Create credentials (API Key)
#    - Add your key to GOOGLE_FACT_CHECK_API_KEY
# 4. Generate secure random keys for SECRET_KEY and JWT_SECRET_KEY
# 5. Configure your database URL for production
# 6. Update CORS_ORIGINS with your frontend domain(s)
#
# Security Notes:
# - Never commit your .env file to version control
# - Use strong, unique keys for production
# - Regularly rotate API keys and secrets
# - Use environment-specific configurations
# ===================================